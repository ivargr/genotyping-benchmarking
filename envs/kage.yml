channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - biopython>=0.1.0
      - npstructures==0.2.0
      - kage-genotyper==0.0.28
      - kmer_mapper==0.0.19
      - obgraph==0.0.25
  - tabix