channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      - biopython>=0.1.0
      - npstructures==0.2.0
      - kage-genotyper==0.1.2
      - kmer_mapper==0.0.20
      - obgraph==0.0.26
      - tabulate
  - tabix